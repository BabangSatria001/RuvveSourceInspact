{
  "name": "source-inspector-netlify",
  "version": "2.0.0",
  "description": "Source code inspector with Netlify Functions",
  "main": "CodeSource.html",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}